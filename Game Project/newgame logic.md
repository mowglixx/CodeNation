# newgame

```
while are_you_sure == false:
  
  ask for player name (input)
  
  if player name entered:
  
    # we have a player name
    ask for text_speed (input)
  
    if text_speed entered:
  
      # we have player name and text_speed
      ask are you sure? (input)

```