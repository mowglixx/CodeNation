from chal0 import drinking_age_checker
from chal1 import password_length
from chal2 import precursor_to_fizzbuzz
from chal3 import fizzbuzz
from chal4 import comparing_letters_from_a_word
from chal5 import time_travel
from chal6 import is_even
from chal7 import is_palindrome
from chal8 import last_vowel
chals = [
    # {"chal_desc": 'Compare variables to numbers (intergers) to satisfy an if condition, num > 18 in this example age has to be greater than 18 and the valid country is "uk"',
    #  "chal_no": 0,
    #  "chal_fn": drinking_age_checker},
    # {"chal_desc": 'Create a variable called password. Check how many letters are in the password, if there are less than 8 print that the password is too short. Otherwise print the password.',
    #  "chal_no": 1,
    #  "chal_fn": password_length},
    # {"chal_desc": 'Create a variable called num. Check if the variable is divisible by 3 or 5. If it is print "This number is divisible by 3 or 5” to the console. Otherwise log “This number is not divisible by 3 or 5”.',
    #  "chal_no": 2,
    #  "chal_fn": precursor_to_fizzbuzz},
    # {"chal_desc": 'Create a variable called num. If num is divisible by 3 print “fizz”, if it’s divisible by 7 print “buzz”, if it’s divisible by both 3 and 7 print “fizz buzz”. Otherwise print num.',
    #  "chal_no": 3,
    #  "chal_fn": fizzbuzz},
    # {"chal_desc": 'Create a variable called word that takes a string. Create an if statement that checks if the last letter is the same as the first. If it is return true, otherwise return false',
    #  "chal_no": 4,
    #  "chal_fn": comparing_letters_from_a_word},
    # {"chal_desc": 'Create a variable called `time`, a variable called `place_of_work` and a variable called `town_of_home`. Create an if statement that prints where someone is at times of the day. E.g. if the time is 7 I’m at home, at 8 I’m commuting, at 9 I’m at work.',
    #  "chal_no": 5,
    #  "chal_fn": time_travel},
    # {"chal_desc": 'Create two variables called num1 and num2. Create an if statement that checks if the result of the sum is even. If it is, return a success message.',
    #  "chal_no": 6,
    #  "chal_fn": is_even},
    # {"chal_desc": 'Create a variable called num. Check if the number is a palindrome (looks the same forward as it does backwards e.g. 1001 or 20202).',
    #  "chal_no": 7,
    #  "chal_fn": is_palindrome},
    {"chal_desc": 'Take the string \'jrfndklhgfndjkjlkgperfijfhdknsadcvjhiiohjfkledsopiuhgtyujwsdxcvhgfdjhiopiwquhejkdsoiufghedjwshi\'. Find the index of a last vowel in the string.',
     "chal_no": 8,
     "chal_fn": last_vowel},
]
